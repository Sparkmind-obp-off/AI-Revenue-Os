{
  "name": "ai-revenue-os-affiliate-prototype",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "typescript": "^5.8.3",
    "vite": "^5.4.19",
    "wrangler": "^4.129.0"
  }
}
